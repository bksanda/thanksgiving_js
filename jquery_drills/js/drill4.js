$(function(){




    console.log('The dom is ready! (drill 4)');

    //1) Hide the first div
    //$('#first').hide();

    //2) Hide the first list in the first div 
    //$('#first > ul.first > li').hide();

	//3) Hide the first list in the second div
	//$('#second > ul.first > li.first').hide();

	//4) Hide the second div
	//$('#second').hide();

	//5) Hide the third item of the third list
	//$('#second > ul.first > li.third').hide();

	//6) Hide the first item of the first list in the first div
	//$('#first > ul.first > li.first').hide();

	//7) Set a red background on the first div
	//$('#first').css('background', 'red');

	//8) Set a red background on the second list in the first div
	//$('#first > ul.second').css('background', 'red');

	//9) Set a red background on the second item of every list
	// Need to figure out the last two...	
	// $('#first > ul.first > li.second').css('background', 'red');
	// $('#first > ul.second > li.second').css('background', 'red');
	// $('#second > ul.first > li.first > ul > li').css('background', 'red');
	// $('#second > ul.first > li.second > ul > li').css('background', 'red');
	// $('#second > ul.first > li.third > ul > li').css('background', 'red');

	//10)Fade out the second div after 5 seconds
	//$('#second').fadeOut(5000);

	//11)Remove the first item of every list from the DOM (no hiding)


	//12)Add a fourth item to the second list in the first div

	//13)Add a fourth item to all three lists (with one function call)

	//14)Remove the second list from the first div and add it back to the second div

	//15)Remove any element that is clicked on

	//16)Remove any element in the first div that is clicked on

	//17) Fade out any element that is clicked, after 3 seconds

});

